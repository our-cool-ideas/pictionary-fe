// Mirrors pictionary-be/src/enum/word-difficulty.ts — kept in sync manually.
// Local to the words module — nothing outside it needs this.
export enum WORD_DIFFICULTY {
  EASY = "easy",
  MEDIUM = "medium",
  HARD = "hard",
}
