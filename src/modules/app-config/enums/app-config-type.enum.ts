// Mirrors pictionary-be/src/enum/app-config-type.ts — kept in sync manually.
// Local to the app-config module — nothing outside it needs this.
export enum APP_CONFIG_TYPE {
  STRING = "string",
  NUMBER = "number",
  BOOLEAN = "boolean",
  JSON = "json",
}
